"""VibeFlow source package.

This file marks `src/` as a Python package, which is what lets
`python -m src.main` and `from src.recommender import ...` resolve reliably.
It is intentionally almost empty: import work belongs in the modules themselves.
"""
